# Copy to .env and adjust as needed.

# Base URL of the SignalSynch FastAPI backend.
VITE_API_BASE_URL=http://localhost:8000

# Set to "true" to run the UI against synthetic contract-shaped data with no
# backend. Leave "false" in real deployments.
VITE_USE_MOCK_DATA=false
